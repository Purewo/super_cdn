# Super CDN Maturity Audit

Last updated: 2026-05-15 Asia/Shanghai.

This document tracks the current maturity work after the staged `v0.4.0` release. It is intentionally evidence-based: do not mark the product mature because a broad test command passed unless the command covers the specific requirement.

## Objective

Make Super CDN mature enough for long-running development and operator use without hiding remaining risk.

Concrete success criteria:

- release gates are repeatable locally and in CI;
- API and CLI behavior have an explicit contract;
- database changes are versioned;
- security and operational mutations have a usable audit trail;
- large server and CLI files have clear ownership boundaries;
- diagnostics explain what to do next, not just what failed;
- manual CDN line switching is explicit, confirmed and auditable;
- rollback paths do not claim to move real traffic unless metadata, Worker assets and KV manifests all move together;
- remaining unsupported paths are clearly documented and not presented as safe apply commands.

## Evidence Checklist

| Requirement | Current artifact | Verification |
| --- | --- | --- |
| Local release gate | `scripts/foundation-check.ps1` | Includes gofmt, PowerShell script syntax validation, actionlint, Go tests/vet/vuln/build, OpenAPI lint, Worker test/typecheck/audit and service healthz. Passed with `-SkipLinuxBuild` using temporary `GOPROXY=https://goproxy.cn,direct` after `proxy.golang.org` EOF. For this run, npm audit passed direct; routing npm audit through `127.0.0.1:10808` produced TLS socket disconnects. |
| CI release gate | `.github/workflows/ci.yml`, `scripts/github-actions-status.ps1` | Defines Go format/test/race/vet/vuln/build, OpenAPI lint, Worker test/typecheck/audit. Workflow static lint is part of the local foundation gate, GitHub action majors are on Node 24-compatible versions, and the status script checks pushed branch/head SHA runs through the GitHub Actions API with optional job/step summaries, dirty-worktree protection and remote branch SHA verification. Recent pushed runs, including `25894299695`, were green for Go test, race, vet, vulnerability scan, build, API contract and Worker jobs; rerun the status script after every new push. |
| Race coverage | `.github/workflows/ci.yml` | CI runs `go test -race ./...` on Ubuntu. Local Windows race remains environment-blocked: `go env` reports `CGO_ENABLED=0`, `CC=gcc`, and no `gcc`, `clang`, `zig` or `cc` command is currently available in PATH. |
| API contract | `api/openapi.yaml` | Redocly lint passed locally through foundation check. |
| Versioned migrations | `internal/db` migration code and tests | Covered by existing DB tests in `go test ./...`. |
| Audit query surface | `GET /api/v1/audit-events`, `supercdnctl audit-log` | Tests cover workspace scoping, viewer rejection and CLI query parameters. |
| Dangerous rollback audit | `site.deployment.promote.rejected` | Tests cover rejected `cloudflare_static` and `hybrid_edge` metadata promote attempts writing audit events. |
| Server ownership boundary | `internal/server/server.go` plus split handler/service files | `server.go` is now route/lifecycle plumbing; feature code lives in narrower files. |
| CLI ownership boundary | `cmd/supercdnctl/main.go` plus command files | `main.go` is dispatcher/global flag plumbing; command groups live in separate files. |
| Deployment target normalization | `internal/deploymenttarget` | Shared by config, server and CLI aliases. |
| Doctor next actions | `cdn-doctor`, `site-doctor` recommendations | Tests cover manual switch recommendations and not-ready paths. |
| Manual switch planning | `supercdnctl switch-plan` | Reports candidate readiness, apply support, risks and next commands. |
| Manual switch apply | `supercdnctl switch-apply` and primary-target APIs | Dry-run by default, requires `-confirm switch`, writes audit events for applied and rejected attempts, returns rollback command. |
| Policy/failover switch boundary | `switch-plan` and `switch-apply` | Policy and resource-failover routes are not presented as directly switchable; server rejects unsupported metadata apply. |
| Cloudflare-backed rollback guard | `promote-deployment`, `delete-deployment` | Non-active `cloudflare_static` and `hybrid_edge` metadata promote attempts return conflict and are audited; `delete-deployment -dry-run` and API `dry_run=true` preview safety/evidence, expose Cloudflare remote cleanup blockers, and deleting those deployments warns that Worker versions, custom domains and KV entries are not removed. |
| Rollback preflight | `supercdnctl rollback-plan` | Read-only plan distinguishes metadata promote from Cloudflare/hybrid redeploy, includes version evidence when available, reports Cloudflare rollback write blockers plus missing evidence, and emits post-redeploy probe commands for Cloudflare/hybrid traffic verification. Live Cloudflare Static canary `supercdn-maturity-static-0515` used `rollback-plan` against `dpl-diirv63ptmqj`, redeployed the historical artifact, and strict `probe-site` passed on the real custom domain. Live hybrid-edge canary `supercdn-maturity-hybrid-ipfs-0515` used `rollback-plan` against `dpl-diirzx0ukg5r`, redeployed the historical artifact, wrote the active KV manifest, and strict `probe-site` passed with HTML from `cloudflare_static` and JS through `ipfs_gateway` manifest routing. |
| Hybrid-edge provider evidence | `deploy-site -target hybrid_edge`, `POST /deployments/{deployment}/hybrid-edge/evidence` | Successful hybrid deployments now record Worker name/version, domains, source assets hash, Workers KV namespace/key prefix, edge manifest hash/size/mode, verification timestamps and entry fallback flags into deployment metadata after strict provider verification. The server writes `site.deployment.hybrid_edge.evidence`; rejected evidence writes are audited separately. `rollback-plan`, `reconcile-deployment`, `deployment` and delete dry-runs can now expose `hybrid_edge` evidence instead of reporting a misleading `cloudflare_static` gap. |
| Cloudflare Static rollback apply | `supercdnctl rollback-apply` | Provider-aware write wrapper for Cloudflare Static rollback. It loads the target deployment, requires a local historical source directory, verifies source assets hash/file count/size against recorded Cloudflare evidence, defaults to dry-run, and with `-dry-run=false -confirm rollback` republishes Cloudflare Static through the same provider flow as `deploy-site` instead of using metadata-only `promote-deployment`. Server records confirmed writes with audit action `site.deployment.cloudflare_static.rollback` and includes both the new deployment id and target deployment id. Local tests cover dry-run evidence validation, source mismatch blocking, confirmed write delegation and rollback audit recording. Commit `9c2b369` passed local foundation and CI run `25896180598`, then was deployed to production with backup `/opt/supercdn/backups/20260515T020403Z-rollback-apply`. Live canary `supercdn-rollback-apply-0515-100952.qwk.ccwu.cc` deployed A `dpl-diivyw5assi5`, deployed B `dpl-diivzds9sjwh`, dry-run verified A rollback, applied rollback as `dpl-diivzj76uo7b`, wrote audit action `site.deployment.cloudflare_static.rollback`, reconciled with `status=ok` / `settled=true`, and confirmed the B marker was absent from live HTML. |
| Cloudflare Static evidence header | generated and augmented `_headers` | Generated Cloudflare Static/Static Assets headers now include `X-SuperCDN-Edge-Source: cloudflare_static`; existing `_headers` files are copied to a temporary deploy directory and augmented unless they already provide that header. Live canary `supercdn-maturity-static-0515.qwk.ccwu.cc` returned the evidence header for both HTML and JS, allowing `probe-site -require-edge-static-html` to verify direct Cloudflare Static traffic instead of relying only on generic Cloudflare response headers. |
| AList/OpenList upload visibility | `internal/storage/alist.go`, live `repo_china_mobile` canary | AList/OpenList stat/get now treats provider `object not found` responses as missing objects, refreshes the parent directory with `/api/fs/list refresh=true`, then retries. This fixed the reproduced `repo_china_mobile` hybrid upload visibility failure. Commit `c2243727223d9ce9bf20a4692ff25797ec2c021e` passed local foundation and CI run `25892297849`, was deployed to production, and live canary `supercdn-maturity-hybrid-0515` deployment `dpl-diit34iw5d3t` passed strict `probe-site -require-edge-static-html -require-edge-manifest-assets` with entry HTML from `cloudflare_static` and JS through the mobile AList manifest route. |
| Provider-state reconciliation | `supercdnctl reconcile-deployment` | Read-only command loads recorded deployment metadata, chooses Cloudflare Static or hybrid-edge strict probe requirements, probes the live production URL, returns `settled`, provider summary, redacted probe evidence and next commands, and never mutates Super CDN or Cloudflare state. It also falls back to recorded Cloudflare evidence URLs/domains for non-active Cloudflare-backed deployments, while warning that shared custom domains may reflect current live state rather than an archived provider version. Live run on `supercdn-maturity-hybrid-0515` deployment `dpl-diit34iw5d3t` returned `status=ok`, `settled=true`, HTML from `cloudflare_static`, and one manifest-routed JS asset through `repo_china_mobile`. |
| Cloudflare Static recovery record | `supercdnctl recover-cloudflare-static`, `POST /cloudflare-static/recoveries` | Evidence validator and recovery recorder for unrecorded Cloudflare Static provider writes. It summarizes the source directory, requires Worker/version/domain evidence, runs a strict Cloudflare Static live probe, and with `-dry-run=false -confirm recover` records a non-active deployment through a recovery-specific server endpoint and audit action. It does not activate recovered deployments. Commit `249ebdc` passed local foundation and CI run `25894299695`, then was deployed to production on 2026-05-15 Asia/Shanghai with backup `/opt/supercdn/backups/20260515T010302Z-recovery-endpoint`; `https://qwk.ccwu.cc/healthz` returned 200 and authenticated `GET /api/v1/sites/supercdn-maturity-static-0515/cloudflare-static/recoveries` returned 405. Live recovery canary `supercdn-recovery-0515-090858.qwk.ccwu.cc` was published through `publish-cloudflare-static` without Super CDN metadata, dry-run verified strict Cloudflare Static evidence, then recorded deployment `dpl-diiuko109n5o` with `active=false`; audit action `site.deployment.cloudflare_static.recovery` was present and `reconcile-deployment` returned `status=ok` / `settled=true`. |
| Cloudflare Static verified activation | `supercdnctl activate-cloudflare-static`, `POST /deployments/{deployment}/cloudflare-static/activate` | Dedicated activation path for recovered Cloudflare Static deployments. It loads the recorded deployment, requires local source hash/file count/size to match stored Cloudflare evidence, runs a strict live probe, defaults to dry-run, and with `-dry-run=false -confirm activate` activates through a Cloudflare-specific audited endpoint. It does not use generic `promote-deployment`. Local tests cover successful activation and rejected evidence mismatches. Commit `bc08ede` passed local foundation and CI run `25895224594`, then was deployed to production with backup `/opt/supercdn/backups/20260515T013045Z-cf-static-activation`; live canary `dpl-diiuko109n5o` dry-run verified, activated to `active=true`, wrote audit action `site.deployment.cloudflare_static.activate`, and `reconcile-deployment` returned `status=ok` / `settled=true`. |
| Operator command copy-paste safety | CLI command hint quoting | Switch and upload diagnostic commands quote PowerShell arguments with spaces or single quotes. |

## Accepted Boundaries

These are intentional product boundaries, not hidden unfinished features:

- Policy-level CDN switching is operator-controlled. `switch-plan` reports readiness and `apply_supported=false` for `routing_policy` and `resource_failover`; `switch-apply` rejects metadata-only policy/failover writes and audits rejected attempts. If this direction changes, use `docs/policy-switching-boundary.md` before adding any write command.
- Metadata delete does not clean Cloudflare Worker versions, custom domains or KV entries. `delete-deployment -dry-run` and API `dry_run=true` now expose `remote_cleanup_supported=false`, blockers and Cloudflare evidence so an operator can perform provider-specific cleanup deliberately.

## Remaining Gaps

These are not solved yet and must not be described as mature:

- Local Windows `go test -race ./...` is still unverified until a working C toolchain is installed; CI is the current race gate. Checked PATH on 2026-05-15 and found no `gcc`, `clang`, `zig` or `cc`.
- Cloudflare Static rollback now has a provider-aware `rollback-apply` write wrapper with live canary coverage. Hybrid-edge rollback is still not implemented as a write operation; the future write-command boundary is recorded in `docs/cloudflare-rollback-boundary.md`.
- Cloudflare provider writes are not fully transactional with Super CDN metadata. A custom-domain propagation delay can make `deploy-site` verification time out after the Worker/domain write has already succeeded; `reconcile-deployment` can inspect recorded Super CDN metadata after a timeout, `recover-cloudflare-static` can record verified unrecorded Cloudflare Static writes as non-active deployments with live canary coverage, `activate-cloudflare-static` provides a live-verified dedicated activation path, and `rollback-apply` covers Cloudflare Static source-verified rollback with live canary coverage. Normal hybrid-edge deployments now persist provider evidence after strict verification, but hybrid-edge writeback after partial failures and hybrid-edge rollback writes are still not implemented as provider-aware writes.

## Next Concrete Work

1. Continue with hybrid-edge writeback or hybrid-edge rollback only when Worker/KV/domain writes are coupled to strict live probes, recorded provider evidence and audit events.
2. If local Windows race coverage is needed, install a working C toolchain and rerun `.\scripts\foundation-check.ps1 -SkipLinuxBuild -Race`; until then, Ubuntu CI is the race gate.
